apiVersion: apps/v1
kind: Deployment
metadata:
  name: quilly-support
  namespace: quilly-support
  labels:
    app: quilly-support
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quilly-support
  template:
    metadata:
      labels:
        app: quilly-support
    spec:
      containers:
        - name: quilly-support
          image: localhost:32000/quilly-support:latest
          ports:
            - containerPort: 8000
          env:
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quilly-support-secret
                  key: ADMIN_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /app/backend/data
            - name: documents
              mountPath: /app/documents
            - name: images
              mountPath: /app/images
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "1"
          livenessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: quilly-data
        - name: documents
          persistentVolumeClaim:
            claimName: quilly-documents
        - name: images
          persistentVolumeClaim:
            claimName: quilly-images
