apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quilly-data
  namespace: quilly-support
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 2Gi
