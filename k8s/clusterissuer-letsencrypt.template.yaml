apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: __ISSUER_NAME__
spec:
  acme:
    email: __LETSENCRYPT_EMAIL__
    server: __ACME_SERVER__
    privateKeySecretRef:
      name: __ISSUER_NAME__-account-key
    solvers:
      - http01:
          ingress:
            class: public
