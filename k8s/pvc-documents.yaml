apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quilly-documents
  namespace: quilly-support
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 1Gi
